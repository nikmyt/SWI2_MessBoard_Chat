export class MessageForm {
    //alright so peep new UNIVERSAL messageForm: destination, timestamp, sender, text, extra
    destination: string; //very important, gets filtered whichever its going, chatroom is assigned to it
    timestamp: string; //
    sender: string;
    text: string;
    extra: string; //just in case?

}